# Base URL of the local Ollama server.
#
# Only the health endpoint uses this today; the deterministic recommender needs
# no external service. The semantic retrieval milestone will use it for the
# nomic-embed-text and qwen2.5:7b models.
OLLAMA_BASE_URL=http://127.0.0.1:11434
